/**
 * Shared details for the legal pages. Play rejects a privacy policy with a
 * placeholder contact under its User Data policy, and India's DPDP Act requires a
 * grievance contact — change `SUPPORT_EMAIL` here and every page follows.
 */

/** Must be a mailbox that is read: reviewers and deletion requests both land here. */
export const SUPPORT_EMAIL = "kumar.abhishek2k23@gmail.com";

/** The name the app is published under, as it appears on the Play listing. */
export const APP_NAME = "Prangan";

/** Hardcoded, not derived from the clock. Bump it when the text changes. */
export const POLICY_LAST_UPDATED = "30 July 2026";
