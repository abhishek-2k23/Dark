import { Stack } from "expo-router";

/** Open stack for developer/design reference screens (component showcase). */
export default function DevLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
